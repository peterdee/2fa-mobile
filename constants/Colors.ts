import { COLORS } from './index';

export default {
  light: {
    text: COLORS.text,
    background: '#FFFFFF',
    tint: COLORS.accent,
    tabIconDefault: COLORS.muted,
    tabIconSelected: COLORS.accent,
  },
  dark: {
    text: COLORS.text,
    background: '#FFFFFF',
    tint: COLORS.accent,
    tabIconDefault: COLORS.muted,
    tabIconSelected: COLORS.accent,
  },
};
